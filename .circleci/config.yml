--- 
jobs: 
  build-job: 
    docker: 
      - 
        image: "circleci/openjdk:8-jdk"
    steps: 
      - checkout
      - 
        restore_cache: 
          keys: 
            - "v1-dependencies-{{ checksum \"build.gradle\" }}"
            - v1-dependencies-
      - 
        run: "gradle dependencies"
      - 
        save_cache: 
          key: "v1-dependencies-{{ checksum \"build.gradle\" }}"
          paths: 
            - ~/.gradle
      - 
        run: "gradle test"
    working_directory: ~/repo
  deploy-job: 
    docker: 
      - 
        image: "circleci/openjdk:8-jdk"
    steps: 
      - 
        run: 
          command: "sudo -H pip install awscli"
          name: "Install awscli"
      - 
        run: 
          command: "aws s3 sync dist s3://circleciwar --delete"
          name: "Deploy to S3 circleciwar bucket"
    working_directory: ~/repo
version: 2
workflows: 
  build-deploy: 
    jobs: 
      - build-job
      - 
        deploy-job: 
          filters: 
            branches: 
              only: master
          requires: 
            - build-job
  version: 2
